# requirements.txt
# AlphaEdge V4 - Pinned dependencies
# Pin minor versions (X.Y.*) to allow patch fixes but block breaking changes
# Run: pip install -r requirements.txt --break-system-packages

# Core data
yfinance==0.2.*
pandas==2.2.*
numpy==1.26.*

# ML models
scikit-learn==1.5.*
xgboost==2.1.*
lightgbm==4.5.*
catboost==1.2.*

# Deep learning (LSTM) — optional, comment out if not using LSTM
# tensorflow==2.17.*   # heavy, only needed for LSTM
# keras==3.5.*

# Dashboard
dash==2.18.*
plotly==5.24.*

# Telegram
python-telegram-bot==21.*

# AI agents
groq==0.11.*   # VetoAgent + CriticAgent

# Sentiment
transformers==4.44.*
torch==2.4.*

# Crypto
ccxt==4.3.*

# Utilities
requests==2.32.*
python-dotenv==1.0.*
